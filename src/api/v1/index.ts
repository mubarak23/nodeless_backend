import { Router } from "express";
//import savedPropertyRouter from "./SavedProperty/saved-property.routes";

const v1: Router = Router();
//v1.use("/saved-properties", savedPropertyRouter);


export default v1;
